# 🌌 Galaksia Teny Gasy (Premium System)

Cette version ajoute **un système premium** (Teknôlôjia) : étoile + planètes avec textures procédurales (CanvasTexture) + atmosphères + anneau plus qualitatif.

Le reste (autres systèmes) reste volontairement simple comme avant.

## Lancer en local
```bash
npm install
npm run dev
```

## Déploiement Vercel
- Push sur GitHub → Import dans Vercel.
- Build: `npm run build`.
- Output: `dist`.

## Notes
- Les textures sont générées localement (pas de téléchargement d'assets), donc c'est plus léger pour mobile.
